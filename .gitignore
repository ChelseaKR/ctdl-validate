.venv/
__pycache__/
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
dist/
build/

# Documents fetched by tools/registry_survey.py. Third parties' published
# records, including contact details; the findings file carries structure and
# counts, never anybody's data. Never commit this. One directory per run.
.registry-cache/
.registry-cache-*/

# Created by the local-run and local-audit instructions in web/README.md:
# `site/` is the assembled playground, `node_modules/` is the pinned audit
# tooling CI installs with --no-save.
site/
node_modules/
